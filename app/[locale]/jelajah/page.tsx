/**
 * `jelajah` is the documented path for the overview; the locale root shows
 * the same screen so arriving at the site lands directly on the doorway.
 */
export { default, generateStaticParams } from '../page'
