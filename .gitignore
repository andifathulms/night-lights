node_modules/
.next/
out/
coverage/
*.tsbuildinfo
.DS_Store
.env*.local

# Raw EOG composites are never committed — CLAUDE.md invariant 9.
data/raw/

# Generated bundle — `pnpm data:build` writes it, CI regenerates it before the export.
public/data/
